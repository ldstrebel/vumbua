[build]
  base = "docs/"
  publish = "_site/"
  command = "bundle install && jekyll build"

[build.environment]
  RUBY_VERSION = "3.2.0"

# Headers for caching
[[headers]]
  for = "/*"
  [headers.values]
    X-Frame-Options = "DENY"
    X-XSS-Protection = "1; mode=block"
